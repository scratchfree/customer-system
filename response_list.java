package morelikes;

public interface response_list {

    // Customer ID (Give)
    void get_customer_id(String cust_id);
    // Customer Response (Give)
    void get_customer_response(String response);
    // Support ID (Give)
    void get_support_id(String sup_id);
}
